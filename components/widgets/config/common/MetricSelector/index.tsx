/**
 * Punto de entrada para el componente MetricSelector
 * Exporta el componente principal como default y named export
 */

import MetricSelectorComponent from "./MetricSelector";

// Exportar componente principal como named export
export const MetricSelector = MetricSelectorComponent;

// Exportar componente principal como default
export { MetricSelectorComponent as default };
